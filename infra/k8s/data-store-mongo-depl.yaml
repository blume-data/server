apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-store-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-store-mongo
  template:
    metadata:
      labels:
        app: data-store-mongo
    spec:
      containers:
        - name: data-store-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: data-store-mongo-srv
spec:
  selector:
    app: data-store-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017