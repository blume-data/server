apiVersion: apps/v1
kind: Deployment
metadata:
  name: database-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: database-mongo
  template:
    metadata:
      labels:
        app: database-mongo
    spec:
      containers:
        - name: database-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: database-mongo-srv
spec:
  selector:
    app: database-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
